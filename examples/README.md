# Plugin Example for autofile

autofile supports a plugin system to add new template fields. 

Edit this example, and the setup.py then install your plugin by running:

`pip install examples/autofile-foo`


